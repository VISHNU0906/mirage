# MIRAGE core runtime (offline / mock mode needs ONLY these).
fastapi>=0.110
uvicorn[standard]>=0.27
PyJWT>=2.8
pydantic>=2.6
Jinja2>=3.1
python-multipart>=0.0.9
requests>=2.31

# Test + dev.
pytest>=8.0
httpx>=0.27

# Optional real LLM providers (NOT needed for mock mode / tests).
# Install only if you set MIRAGE_LLM_PROVIDER=gemini / openai.
# google-generativeai>=0.5
# openai>=1.30
